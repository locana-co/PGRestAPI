extends layout

block content
	p
		| Table Listing

	table
		thead
			tr
				th Name
		tbody
			- var items = list
			- each item in items
				tr
					td
						a(href=url + "/" + item.table_name) #{item.table_name}
						


block sidebar
	.widget